services:
  python-service:
    build:
      context: ./python-service
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    networks:
      - stats-net

networks:
  stats-net:
    driver: bridge

    